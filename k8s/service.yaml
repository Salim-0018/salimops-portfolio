apiVersion: v1
kind: Service

metadata:
  name: salimops-portfolio-service

spec:
  type: NodePort

  selector:
    app: salimops-portfolio

  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
